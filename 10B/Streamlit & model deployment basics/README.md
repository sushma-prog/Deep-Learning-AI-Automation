# 🖼️ CNN Image Classifier (Streamlit App)

This project is a **Convolutional Neural Network (CNN) Image Classifier** wrapped in a **Streamlit web app**.
Users can upload an image, and the model will predict the class (CIFAR-10 dataset classes).

---

## 📌 Features

* Upload an image (`jpg`, `jpeg`, `png`)
* Preprocess the image automatically
* Run prediction using the trained CNN model
* Display predicted class with confidence

---

## 📂 Project Structure

```
.
├── app.py                     # Streamlit app
├── cnn_image_classifier.keras # Trained CNN model
└── README.md                  # Project documentation
```

---

## ⚙️ Requirements

To run this project, install the following Python libraries:

```
streamlit
tensorflow
numpy
Pillow
```
